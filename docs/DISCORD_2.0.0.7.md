# :rocket: GW2 In-Game Helper — **v2.0.0.7**

> Fix: clicks no longer go through the overlay into the game (skills / camera).

**Grab it:** [Latest DLL](https://github.com/Xydroc-IO/GW2-InGame-Helper/releases/latest/download/GW2-InGame-Helper.dll) — drop into `<Guild Wars 2>/addons/`. Restart GW2 after updating.

---

## What's new

- **Click-through fixed** — WndProc now eats mouse down/up/move/wheel while the pointer is over the helper
- **Collapsed bar** — title bar still blocks clicks when the window is collapsed
- **Drag latch** — a press that starts on the overlay keeps capture until button-up even if the cursor leaves

`Ctrl+Shift+H` · *Xydroc*
