#pragma once

/* Draws enabled Tekkit trails/markers over the stock GW2 compass using
   MumbleLink compass size, corner, rotation, and map center/scale. */

namespace CompassOverlay
{
	void Render();
}
